CREATE OR REPLACE PROCEDURE DBVESTIB.SP_FORMATA IS

	RESULTADO LONG;
	CODAL     NUMBER;
BEGIN
	CODAL := 1;
	WHILE CODAL < 419489 LOOP
		BEGIN
			RESULTADO := DBVESTIB.F_FORMATA_TELEFONE(CODAL);
			CODAL     := CODAL + 1;
		END;
	END LOOP;
END SP_FORMATA;

